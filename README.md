# business-plus
    
    
   1.   install npm. 
   
            `$ npm install`
            
   2. install bower.
   
            `$ npm install -g bower`
            
   3. install ruby and sass.
   
            `$ gem install sass`
               
  4. install pug.   
   
           `$ npm install pug`
           `npm install pug-cli -g`
           
  
        
        
   
        